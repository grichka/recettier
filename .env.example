# Google OAuth Configuration - only Client ID is needed
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
# Note: Users will provide their own API key through the app interface

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Google OAuth credentials
# 3. Make sure your Google OAuth app is configured for:
#    - Authorized JavaScript origins: http://localhost:3000
#    - Authorized redirect URIs: http://localhost:3000/recettier/
# 4. Enable the Google Drive API in your Google Cloud Console